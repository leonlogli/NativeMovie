import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: { flex: 1, position: 'relative', padding: 16 },
  columnWrapperStyle: {
    gap: 16,
  },
  movieCard: {
    flex: 1 / 2,
    marginVertical: 8,
  },
});

export default styles;
