import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 16,
    gap: 32,
  },
  card: {
    padding: 16,
    borderRadius: 32,
  },
  title: {
    textAlign: 'center',
    marginBottom: 16,
    opacity: 0.75,
  },
  overview: {
    fontSize: 16,
    textAlign: 'center',
    lineHeight: 24,
  },
  distribution: {
    textAlign: 'center',
    marginTop: 16,
    opacity: 0.75,
  },
});

export default styles;
