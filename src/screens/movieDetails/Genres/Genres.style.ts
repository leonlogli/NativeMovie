import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 16,
    gap: 32,
  },
  card: {
    padding: 16,
    borderRadius: 32,
  },
  overview: {
    marginTop: 16,
    fontSize: 16,
    lineHeight: 24,
  },
  distribution: {
    marginTop: 16,
    opacity: 0.75,
  },
});

export default styles;
